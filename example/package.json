{
  "name": "force-focus-example",
  "version": "1.0.0",
  "description": "An example application to demonstrate force focus",
  "main": "main.js",
  "scripts": {
    "example": "electron ./",
    "rebuild-electron": "electron-rebuild -f"
  },
  "devDependencies": {
    "forcefocus": "..",
    "electron": "^14.0",
    "electron-rebuild": "^3.2.3"
  }
}
